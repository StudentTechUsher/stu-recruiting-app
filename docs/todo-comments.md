// TODO(JAROM): Improve the onboarding experience for students; definition of done is having someone at Sandbox complete onboarding in 60 seconds.
// TODO(JAROM): Improve color usage on the artifact repository and ensure colors have clear meaning.
// TODO(JAROM): For a user's first time using source extraction in the artifact repository, ensure this is topmost and has clear process-complete feedback plus clear success/error snackbars.
// TODO(JAROM): On the profile page, verify that LinkedIn and GitHub still link to the user's profile after artifact repository extraction methods are used.

// TODO(VIN): Wire recruiter views to Greenhouse read-only data and the database to begin recommendation process discovery.
// TODO(VIN): Investigate v1 for creating capability models.
// TODO(VIN): Connect pipeline overview to available ATS and stu recruiting candidate data, using BambooHR for first-attempt modeling.
// TODO(VIN): Build a very basic candidate explorer that displays all available student profiles.
// TODO(VIN): Redesign and implement a basic v1 of the candidate relationship manager.
